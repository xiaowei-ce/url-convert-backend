mode:
  type: Standalone
dataSources:
  master:
    dataSourceClassName: com.zaxxer.hikari.HikariDataSource
    driverClassName: com.mysql.cj.jdbc.Driver
    jdbcUrl: jdbc:mysql://debian-server:3307/url-convert?allowPublicKeyRetrieval=true
    username: root
    password: wei5877261
  slave:
    dataSourceClassName: com.zaxxer.hikari.HikariDataSource
    driverClassName: com.mysql.cj.jdbc.Driver
    jdbcUrl: jdbc:mysql://debian-server:3308/url-convert?allowPublicKeyRetrieval=true
    username: root
    password: wei5877261

rules:
  - !SINGLE
    tables:
      - "*.*"
  - !READWRITE_SPLITTING
    dataSourceGroups:
      readwrite_ds:
        writeDataSourceName: master
        readDataSourceNames:
          - slave
        loadBalancerName: random
        transactionalReadQueryStrategy: PRIMARY #事务内读主库
    loadBalancers:
      random:
        type: RANDOM
props:
  sql-show: true